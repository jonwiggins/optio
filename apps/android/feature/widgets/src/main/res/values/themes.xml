<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window themes only; the screens are Compose (OptioTheme). -->
    <style name="Theme.OptioWidgets" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- The run-shortcut confirmation: only its dialog shows, over whatever was on screen. -->
    <style name="Theme.OptioWidgets.Translucent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
    </style>
</resources>
