package dev.optio.feature.work

import androidx.navigation3.runtime.EntryProviderScope
import androidx.navigation3.runtime.NavKey

/**
 * Registers `:feature:work`'s routes (none yet: Work rows open other features' detail routes,
 * e.g. `TaskDetailRoute`, `LocalTerminalRoute`, `AgentDetailRoute`).
 */
fun EntryProviderScope<NavKey>.workEntries() {
    // No routes yet.
}
