/**
 * `:core:workfeed`: the merged Work feed (port of iOS `WorkFeed` / web `lib/work-feed.ts`),
 * shared by the Work list, Overview, and the glanceable surfaces. Owned by Agent A1 (PLAN §5).
 */
package dev.optio.core.workfeed
