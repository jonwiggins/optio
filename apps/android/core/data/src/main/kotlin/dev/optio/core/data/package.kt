/**
 * `:core:data`: `ServerProfile`, `ServerRegistry` (DataStore), `TokenStore` (Keystore),
 * `SessionStore`, `CurrentUser`, `DeepLink`. Owned by Agent C (PLAN §3, §5).
 */
package dev.optio.core.data
