/**
 * `:core:terminal`: `OptioTerminalView` (Termux `TerminalEmulator` + `TerminalRenderer` in our
 * own View) with a Compose wrapper, `TerminalKeyBar`, `TerminalTheme`, `TerminalSizing`,
 * `StreamPolicy`. Depends on Android + the Termux libraries only. Owned by Agent X (PLAN §3, §5).
 */
package dev.optio.core.terminal
