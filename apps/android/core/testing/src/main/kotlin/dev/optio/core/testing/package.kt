/**
 * `:core:testing`: Robolectric + Roborazzi screenshot helpers, MockWebServer helpers
 * (`FakeOptioServer`), sample model builders. Used via `testImplementation(projects.core.testing)`
 * (the feature convention plugin adds it automatically). Owned by Agent U (PLAN §3, §5).
 */
package dev.optio.core.testing
