/**
 * `:core:glance`: needs-you snapshots and Watch state shared by widgets, tiles, the ongoing
 * notification and periodic refresh (ports of iOS `NeedsYouSnapshot` / `WatchActivity`).
 * Owned by Agent A9 (PLAN §5).
 */
package dev.optio.core.glance
